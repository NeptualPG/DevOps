YAML: IS the lenguaje used for create and reproduces 
automations those are into the most programming apps

name: workflow ritle
on: Defines triggers 
jobs: jobs and steps define
steps: sequential commands or actions
run: shell commands to execute 
uses: use prebuilt actions
with: pass params to actions
env: set enviroment variables
needs: make one job depend on another 
